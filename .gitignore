# dependencies
node_modules/
.pnp
.pnp.js

# builds / output
dist/
build/
out/
.next/
.vinext/
.open-next/

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
worker-configuration.d.ts

# env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
bun-debug.log*

# misc
.DS_Store
.turbo/
.tmp/
coverage/
*.tsbuildinfo

# vinext regenerates apps/web/worker/index.ts on every deploy (image-opt entry).
# Our wrangler `main` points at vinext's app-router-entry, so this copy is an
# unused build artifact — ignore it so it can't drift or break the gate.
apps/web/worker/
