{
  "name": "@manual.email/egress",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 10140 --inspector-port 10141 --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --port 10140 --inspector-port 10141 --persist-to ../../.wrangler/state",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "@manual.email/contracts": "workspace:*",
    "@manual.email/db": "workspace:*"
  }
}
