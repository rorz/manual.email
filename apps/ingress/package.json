{
  "name": "@manual.email/ingress",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 10130 --inspector-port 10131 --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --port 10130 --inspector-port 10131 --persist-to ../../.wrangler/state",
    "cf-typegen": "wrangler types",
    "seed": "bun scripts/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "@manual.email/contracts": "workspace:*",
    "@manual.email/db": "workspace:*",
    "drizzle-orm": "catalog:"
  }
}
