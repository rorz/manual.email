{
  "name": "@manual.email/ingress",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 10130 --inspector-port 10131",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --port 10130 --inspector-port 10131",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply manual-email --local",
    "db:migrate": "wrangler d1 migrations apply manual-email --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.97.0"
  },
  "dependencies": {
    "@manual.email/contracts": "workspace:*"
  }
}
