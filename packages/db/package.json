{
  "dependencies": {
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "better-auth": "catalog:",
    "drizzle-kit": "catalog:",
    "wrangler": "catalog:"
  },
  "exports": {
    ".": "./src/index.ts",
    "./address": "./src/address.ts",
    "./client": "./src/client.ts",
    "./keys": "./src/keys.ts",
    "./schema": "./src/schema.ts"
  },
  "name": "@manual.email/db",
  "private": true,
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply manual-email --local --persist-to ../../.wrangler/state",
    "db:migrate:prod": "wrangler d1 migrations apply manual-email --remote",
    "db:seed:local": "bun scripts/seed-local-signup-code.ts",
    "db:splat:local": "bun scripts/splat-local.ts",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "version": "0.0.0"
}
